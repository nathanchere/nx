# nx

Just some scripts for helping me automate the process of setting up a new Linux box

Only tested on Ubuntu Mate 16.04

Edit `options.sh` to decide what packages/etc to install

To run, once cloned somewhere locally:

```
chmod +755 ./run.sh
./run.sh
```
